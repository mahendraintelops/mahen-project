apiVersion: v1
kind: Service
metadata:
  namespace: test-service
  labels:
    app: test-service
  name: test-service
spec:
  ports:
  
    - protocol: TCP
      port: 4444
      targetPort: 4444
   
   
  selector:
    app: test-service
  type: ClusterIP
